#!/usr/bin/python3
def remove_char_at(str, n):
    if 0 <= n < len(s):
        result = ""
        for i in range(len(S)):
            if i != n:
                result += s[i]
        return result
    else:
        return s
